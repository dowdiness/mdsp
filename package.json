{
  "name": "moondsp-browser-tools",
  "private": true,
  "scripts": {
    "build:browser": "moon build browser --target wasm-gc --release",
    "test:browser": "moon build --target wasm-gc --release && ./playwright-sync-wasm.sh && playwright test",
    "test:browser:headed": "moon build --target wasm-gc --release && ./playwright-sync-wasm.sh && playwright test --headed",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "verify:composition-authoring": "NEW_MOON_MOD=0 moon build cmd/composition_prepare --target js --release && node scripts/composition-definition-check.mjs",
    "prototype:composition": "NEW_MOON_MOD=0 moon build cmd/composition_prepare --target js --release && NEW_MOON_MOD=0 moon build browser/direct_audio --target js --release && python3 scripts/audio-profile-server.py",
    "prototype:composition-cli": "NEW_MOON_MOD=0 moon build cmd/composition_prepare --target js --release && node scripts/composition-prototype.mjs",
    "verify:composition": "NEW_MOON_MOD=0 moon build cmd/composition_prepare --target js --release && node scripts/composition-check.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "wrangler": "^4.90.0"
  }
}
